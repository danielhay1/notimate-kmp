interface:
  display_name: "NotiMate KMP PR Reviewer"
  short_description: "Independent senior KMP and Android PR review"
  default_prompt: "Use $notimate-kmp-pr-reviewer to review this pull request, attach every validated blocker, should-fix finding, and nit to the relevant diff line, and provide the required merge-readiness summary."

policy:
  allow_implicit_invocation: true
